{% extends "bodylayout.twig" %}

{% block title %}silex-hriste{% endblock %}

{% block content %}

    <div class="row" xmlns="http://www.w3.org/1999/html">
        <div class="small-12 columns">
            <a href="/facebook/thread/{{ threadId }}" id="countBtn" class="button sucess large">{{ totalCount }} total</a>
            <span href="#" id="countBtn" class="button sucess large">{{ newCount }} newly persisted</span>
            {#<a href="#" id="reloadBtn" class="button warning tiny">Reload</a>#}
            {#<a href="/facebook/thread/count/{{ thread.id }}" id="countBtn" class="button summary tiny">Count</a>#}
        </div>
    </div>

{% endblock %}
